MASTER_PLANNING_PLANNING = """

你是一个任务规划系统。你只能使用以下可用的机器人：{robot_name_list}

每个机器人具备以下能力：{robot_tools_info}

你在分解任务时，必须参考以下场景信息：
{scene_info}

## 场景信息说明：
- 每个物体都有 "position" 字段，显示其实时坐标，格式为 "(x, y, z)"
- "grasped: true" 表示该物体当前已被机器人抓取
- 家具（counter、island、stove、sink、cabinet）是固定位置
- "graspable: false" 的物体（如 plate 盘子等容器/放置目标）**不能被抓取或搬动**，它已在场景里就位，只能作为放置目标把别的物体放到它上面

## 放置目标规则（重要）：
- 当任务是"把 A、B、C 放到 X 上"（X 如 plate 盘子、bowl 碗、某家具）时，X 是**放置目标**，已经就位。
- **绝对不要**为放置目标 X 生成 "搜索并抓取 X" / "把 X 放到某处" 之类的子任务——X 不需要被抓取或移动。
- 每个要放置的物体各自走："搜索并抓取 A → 导航到 X → 将 A 放到 X 上"，B、C 同理。

## 导航规则：
- 导航时必须使用物体名称或家具名称，不要使用原始坐标
- 正确示例：navigate_to_target(target="apple")
- 错误示例：navigate_to_target(target="(1.63, -0.31)")
- 系统会自动找到离目标最近的工作点
- 到达工作点后，机器人可以用机械臂够到附近物体

## 关键要求：
1. 你必须使用上面列表中的确切机器人名称：{robot_name_list}
2. 不要编造或使用通用名称，如 "robot"、"agent" 等
3. 你输出的每个 "robot_name" 必须与可用机器人完全一致
4. 物体名称必须使用英文，与 scene_info 中完全一致（如 "apple"、"cup"、"counter"）
5. navigate_to_target 的 target 参数必须是物体或家具的英文名称，不能是坐标

请将给定任务分解为子任务，每个子任务不能过于复杂，确保单个机器人可以完成。

## 分解指南：

- 分析任务，根据机器人的可用工具将其分解为逻辑步骤。
- 如果任务涉及移动到某个位置并在那里执行操作，请分解为独立的子任务（例如：先导航，再操作）。
- 导航时使用物体名称，不使用坐标。
- 如果任务足够简单，单个工具调用即可完成（例如：已在目标位置时"抓取方块"），可以作为一个子任务。
- 在决定是否需要导航时，务必考虑机器人当前位置和场景布局。

输出的每个子任务需要简明扼要，并包含完成该子任务的机器人名称。
此外，你需要给出子任务分解的推理说明，并分析每一步是否可以由单个机器人基于其工具来完成！

## 输出格式如下，采用 JSON 结构：
{{
    "reasoning_explanation": xxx,
    "subtask_list": [
        {{"robot_name": xxx, "subtask": xxx, "subtask_order": xxx}},
        {{"robot_name": xxx, "subtask": xxx, "subtask_order": xxx}},
        {{"robot_name": xxx, "subtask": xxx, "subtask_order": xxx}},
    ]
}}

## 说明：'subtask_order' 表示子任务的执行顺序。
如果任务之间没有先后依赖关系，请为这些任务设置相同的 'subtask_order'。例如，两个机器人分别执行两个独立的任务，它们应共享相同的 'subtask_order'。
如果任务之间有先后顺序，则 'subtask_order' 应按执行顺序递增。例如，task_2 必须在 task_1 之后开始，它们应有不同的 'subtask_order'。

{experience_section}
# 待完成的任务是：{task}。你的输出：
"""

EXPERIENCE_CLASSIFY_SUCCESS = """你是一个机器人操作经验分类助手。用户总结了一次成功任务的经验，请判断它属于哪个技能类别，并提炼成一条可复用的操作策略。

当前任务背景：{task_desc}
用户总结的成功经验：{raw_input}

可用技能类别：
- navigate: 导航移动相关（路径规划、绕障、手持物体时移动注意事项等）
- grasp: 抓取相关（抓取位置、角度、物体特性等）
- place: 放置相关（放置位置、高度、顺序等）
- multi_step: 多步骤任务相关（任务顺序、步骤衔接、整体策略等）

输出 JSON（不要其他内容）：
{{
    "skill": "place",
    "tip": "提炼后的操作策略，35字以内，直击要点，以动词开头"
}}"""

EXPERIENCE_CLASSIFY = """你是一个机器人操作经验分类助手。用户描述了一次失败的经验教训，请判断它属于哪个技能类别，并提炼成一条清晰的避免规则。

当前任务背景：{task_desc}
用户描述的失败经验：{raw_input}

可用技能类别：
- navigate: 导航移动相关（路径规划、绕障、手持物体时移动注意事项、防碰撞等）
- grasp: 抓取相关（抓取位置、角度、力度、物体特性等）
- place: 放置相关（放置位置、高度、角度、避免碰到其他物体等）
- multi_step: 多步骤任务相关（任务顺序、步骤衔接、整体策略等）

输出 JSON（不要其他内容）：
{{
    "skill": "place",
    "rule": "提炼后的避免规则，35字以内，直击要点，以动词开头"
}}"""

EXPERIENCE_GENERATION = """你是一个经验总结助手。请根据以下任务执行信息，生成一条有针对性的经验规则。

任务：{task}
子任务执行情况：
{subtask_summary}
用户反馈：{feedback_type}
{user_note_section}

【ALFWorld 操作骨架——硬性约束，违反即视为错误经验】
- 放置（put/move X to Y）的骨架**恒为**：先"导航到 Y" → 再"move X to Y"，两步缺一不可。
  原因：抓取阶段（搜索并抓取 X）会把机器人带到 X 所在位置（常是 cabinet/drawer），
  抓到后机器人**不在**目标 Y 处，必须先导航回 Y 才能放。
- **严禁**产出任何"放置无需导航/无需移动/直接放置即可/已在目标位置可省略导航"之类的经验——
  即使本次执行时机器人碰巧在 Y，也不能泛化成"以后都不用导航"，那会让下次规划漏掉导航步而失败。
- 放置类子任务失败（move X to Y 返回 Nothing happens）时，根因**优先判定为"机器人不在 Y"**
  （搜索阶段已移动到别处），修复办法是"放置前补一个导航到 Y"，**不要**归因为"目标被占用/无空位"。

要求：
- 负向反馈时，说清楚【哪一步失败了】+【应该怎么改】，格式："XXX失败原因是YYY，下次应先ZZZ"
- 正向反馈时，总结一条可复用的操作规律（但不得违反上面的骨架约束）
- 如果子任务中有"在XXX找不到YYY"类的观测，要明确指出那个位置为空，并建议搜索其他位置
- 控制在60字以内，直击要点，不能只说"需要确认持有物体"这种空话，要说具体位置和动作

输出格式（纯文本，不要JSON，不要markdown）：
一条具体的经验规则"""
